# parcel learning

> one demo with typescript && docker running

## how to runnning

* local

```code
yarn 
yarn start (live build)
yarn build (product)
```

* docker

```code
docker-compose  build
docker-compose up -d
```